{
  "framework": "nextjs",
  "buildCommand": "prisma generate && next build",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/cron/advance-rounds",
      "schedule": "* * * * *"
    }
  ]
}
