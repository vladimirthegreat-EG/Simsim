/**
 * 4-Team Strategy Differentiation Test
 *
 * This script tests that different strategies produce meaningfully different outcomes.
 * Run with: npx tsx scripts/strategy-test.ts
 *
 * NOTE: This script uses a simplified approach and may need updates to match current engine APIs.
 */

// @ts-nocheck - This script needs major updates to match current interfaces

import { SimulationEngine } from "../engine";
import type { TeamState, MarketState, AllDecisions, Segment } from "../engine/types";

console.log("\n⚠️  WARNING: This script (strategy-test.ts) is currently outdated and needs updates.");
console.log("The engine interfaces have evolved significantly.");
console.log("Please use the test files in __tests__/strategy/ instead.\n");

process.exit(0);
